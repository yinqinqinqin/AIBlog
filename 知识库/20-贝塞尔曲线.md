# 贝塞尔曲线

## DCC中的贝塞尔曲线

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543944.png)

### 空间插值

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543939.png)

### 求解方式

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543933.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528020.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543937.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152556885.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528018.png)

```javascript
function solveParams(x0, x1, x2, x3){
        return {
            a:-x0+3*x1-3*x2+x3,
            b:3*x0-6*x1+3*x2,
            c:-3*x0+3*x1,
            d:x0
        };
    }
    function getOnly(possible_t){
        const eps = 1e-10
        for(var i in possible_t){
            var t = possible_t[i];
            if(t >= -eps && t <= 1+eps){
                return Math.max(Math.min(t, 1.0),0.0);
            }
        }
        return null;
    }
    function calBezier(v0, v1, v2, v3, t0, dim){
        var t1 = 1-t0;
        var v = [0, 0, 0];
        for(var i=0;i<dim;i++)
            v[i] = v0[i]*t1*t1*t1 + 3*v1[i]*t1*t1*t0 + 3*v2[i]*t1*t0*t0 + v3[i]*t0*t0*t0;
        return v;
    }

    function solveSeg(p0, p1, p2, p3, x, dim){
        var params = solveParams(p0.x, p1.x, p2.x, p3.x);
        //var params_y = solveParams(p0[1], p1[1], p2[1], p3[1]);
        var possible_t = solveEquation(params.a, params.b, params.c, params.d-x);
        var t = getOnly(possible_t);
        if(t == null)
            return null;
        return calBezier(p0.v, p1.v, p2.v, p3.v, t, dim);
    }
    function solveEquation(a, b, c, d){
        var x1 = null;
        var x2 = null;
        var x3 = null;

        var A = b * b - 3 * a * c;
        var B = b * c - 9 * a * d;
        var C = c * c - 3 * b * d;
        var delta = B * B - 4 * A * C;
        if(A == 0 && B == 0){
            //x1 = x2 = x3 = -b / (3 * a);
            x1 = -b / (3 * a);
            x2 = -c / b;
            x3 = -3 * d / c;
            return [x1];
        }
        else if(delta > 0){
            var Y1 = A * b + 3 * a * (1 / 2 * (-B + Math.sqrt(delta)));
            var Y2 = A * b + 3 * a * (1 / 2 * (-B - Math.sqrt(delta)));

            Y1 = Math.sign(Y1) * Math.pow(Math.abs(Y1), 1/3);
            Y2 = Math.sign(Y2) * Math.pow(Math.abs(Y2), 1/3);

            x1 = (-b - Y1 - Y2) / (3 * a);
            x2 = [
                (-2 * b + Y1 + Y2) / (6 * a),
                Math.sqrt(3) * (Y1 - Y2) / (6 * a)
            ];
            x3 = [
                x2[0],
                -x2[1]
            ]
            return [x1];
        }
        else if(delta == 0){
            x1 = -b / a + B / A;
            x2 = -B / (2 * A);
            x3 = x2;
            return [x1, x2];
        }
        else
        {
            var T = (2 * A * b - 3 * a * B) / (2 * Math.sqrt(A * A * A));
            var theta = Math.acos(T);
            x1 = (-b - 2 * Math.sqrt(A) * Math.cos(theta / 3)) / (3 * a);
            x2 = (-b + Math.sqrt(A) * (Math.cos(theta / 3) + Math.sin(theta / 3) * Math.sqrt(3))) / (3 * a);
            x3 = (-b + Math.sqrt(A) * (Math.cos(theta / 3) - Math.sin(theta / 3) * Math.sqrt(3))) / (3 * a);
            return [x1, x2, x3];
        }
    } var p0 = {x:0, v:[0]};
        var p3 = {x:1, v:[210]};
        var width = p3.x-p0.x;
        var f = 210/Math.sqrt(210*210+240*240);
        var p1 = {x:p0.x + width * 100 / 100, v:[p0.v[0] + (20 *f * width * 100 / 100)]};
        var p2 = {x:p3.x - width * 10 / 100, v:[p3.v[0] - (369.610964559071 *f * width *  10 / 100)]};

        var p = solveSeg(p0, p1, p2, p3, time, 1);
        JSON.stringify((p))
```

### 时间插值

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543945.png)

x,y=f(s)

s=g(t)

### 时间求解

如何得到贝塞尔曲线的曲线长度和 t 的近似关系? - 知乎

首先做完这个曲线上匀速运动的编程实现后，是十分愉悦的。这个问题里，两个科学史上的伟人，牛顿和高斯，相继大显身手，怎能不让人膜拜？我之前并不了解所涉及的数值分析方法，所以觉得很神奇。

以下为简洁我会使用向量的表示方法，使用大写数字代表向量。以catmullrom样条曲线(spline)为例，对于一个关于t的多次样条曲线:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152556884.png)

一般来说实际中，t的范围都是[0, 1]。当t在[0, 1]中滑动时，Q(t)的集合即为一条平滑曲线。注意到当t以匀速移动时，Q(t)在沿曲线方向上并不是匀速移动。要做到这一点，显然曲线长度是需要考虑的。假设在t点处我们有曲线长度

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543952.png)

那么s长度所对应的t即为上面方程的反函数:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543942.png)

反带入Q(t)，得

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543934.png)

s的区间显然是0到曲线长度，于是我们可以对s做匀速递增从而拿到Z(s)的位置， 从而完成匀速插值。这就是整个思路，难点在于求S=L(t)，这实际上是一个求积分的问题。很遗憾，一般来说此处该问题你是很难得到一个解析的积分公式的，实际中是通过数值计算方法得到该积分值。

而既然积分值s我们是用的数值方法得到，那很显然求该积分函数的逆，即给定的s对应的t，也只能通过数值方法得到了，我们将会使用牛顿迭代法。

该积分我们使用Guass-lengendre积分来实现，通过选定的几个积分点及权重，它神奇而快速的得到精度很高的积分值。我们直接来重点吧，该方法需要一张n阶权重表，比如5阶权重表:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543950.png)

及一个计算公式:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528023.png)

> Gauss-Legendre积分公式

为了应用该公式，我们有:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528017.png)

Q'(t)为Q(t)的导，||Q'(t)||为该点处的长度:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528026.png)

具体对于我们的公式，有:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543946.png)

将表中的值依次带入，迭代计算即可得到t点处的积分值s。比我之前快速实现时所采用的线性分割方法快了100倍有没有？特别是该方程的普适性，对于多阶多项式都适用而且精度非常高。有种魔性在这里，高斯是神。

现在s=L(t)对我们来说是已知了，那么如何求s所对应的t呢?这实际上是求方程:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528021.png)

的根。因为L(t)没有解析表达，我们将使用牛顿迭代法:

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543935.png)

将得到的b的值作为a再带入上述公式，迭代几次即可逼近s = f(x)的根。具体到我们的公式：

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543951.png)

对于特定的s，我们首先要拟定一个初值a，因为样条曲线性质良好，而且一般说来实际中不会允许2点间出现陡峭的曲线，一定会多加一些点令2点间曲线形状为凸或凹的，所以我们可以近似认为t在区间[0, 1]的比例，近似等于s与总长度的比例。而显然总长为L(1),通过前面的高斯积分已经可以算出，于是初始值a我们可拟定为：

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543941.png)

反复迭代(3-4次的精度就不错了)，得出最终t值，再把该值反带入Q(t)中，得到对于给定s值所对应的位置，至此我们完成了样条曲线的重参数化。

### 常用预设

Easing functions specify the speed of animation to make the movement more natural. Real objects don’

## 贝塞尔曲线自适应分割

adaptive_bezier

http://antigrain.com/research/adaptive_bezier/

https://www.cnblogs.com/muxue/archive/2010/07/04/1771034.html [https://www.cnblogs.com/muxue/archive/2010/07/04/1771034.html](https://www.cnblogs.com/muxue/archive/2010/07/04/1771034.html)

https://www.cnblogs.com/muxue/archive/2010/07/04/1771040.html [https://www.cnblogs.com/muxue/archive/2010/07/04/1771040.html](https://www.cnblogs.com/muxue/archive/2010/07/04/1771040.html)

### 使用步长

```
 /*
Four control point Bezier interpolation
mu ranges from 0 to 1, start to end of curve
*/XYZ Bezier4(XYZ p1,XYZ p2,XYZ p3,XYZ p4,double mu)
{
    double mum1,mum13,mu3;
    XYZ p;
    mum1 = 1 - mu;
    mum13 = mum1 * mum1 * mum1;
    mu3 = mu * mu * mu;
    p.x = mum13*p1.x + 3*mu*mum1*mum1*p2.x + 3*mu*mu*mum1*p3.x + mu3*p4.x;
    p.y = mum13*p1.y + 3*mu*mum1*mum1*p2.y + 3*mu*mu*mum1*p3.y + mu3*p4.y;
    p.z = mum13*p1.z + 3*mu*mum1*mum1*p2.z + 3*mu*mu*mum1*p3.z + mu3*p4.z;
    return(p);
}
```

> 首先，这个方法很慢，2D 的情况下，每个点的计算需要进行 24 次浮点数乖法。不过这个问题可以很容易的解决，我们可以使用递增的方法代替这种直接计算，在这篇文章的最后有相关的描述：Interpolation with Bezier Curves，你可以看到，主循环中只有 6 次加法操作。就我所知，这是目前最快的方法，特别是在现代的处理器上更是如此，因为这些处理器对浮点数的计算都已经相当的快了。 [http://www.antigrain.com/research/bezier_interpolation/index.html#PAGE_BEZIER_INTERPOLATION](http://www.antigrain.com/research/bezier_interpolation/index.html#PAGE_BEZIER_INTERPOLATION)
>
> 但主要的问题是如何决定中间点的数目，还有如何对 mu 值进行递增。最简单的方法是选择某个步进，比如，每次增加 0.01 ，这样任何曲线都会被分成 99 条线段。当然，缺点也显而易见，对于长曲线中间点太少，而对于短曲线中间点则太多。
>
> 很显然，我们应该基于曲线的长度来计算步进，但计算曲线长度又要求我们能计算出曲线本身，于是，在这里我们陷入了经典的“第二十二条军规”的尴尬矛盾中（译注：因为我们对曲线的长度和形状都是不确定的，所以用线段来近似它，但最佳的线段的数目和长度最要求我们知道曲线长度和形状）。（所以，为此，我们要找别的方法），对于长度，采用有一个相当不错的估算方法：
>
> (p1,p2)+(p2,p3)+(p3,p4);

#### 可能情况

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528013.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152556883.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543947.png)

#### 理想情况

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528015.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543949.png)

### Paul de Casteljau Divides and Conquers

#### 递归分割

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528016.png)

> 我们有 1,2,3,4 四个控制点。计算出它们的中间 12, 23, 34 ，以及“高一阶”的中点 123, 234 ，最后一阶的中间 1234 就落在曲线上了。这里也可以不用中点(系数 t ＝ 0.5)，而使用其它的 0 到 1 之间的系数。但在这篇文章中，我们使用中点。产生的两条新曲线与原曲线完全一致，但新曲线的控制点变成了：1,12,123,1234（左半边），和 1234,234,34,4（右半边）。很明显，“新”的曲线比原来的曲线要平坦一点，所以，如果我们重复这个过程若干次，我们就可以把用线段来代替曲线。

```cpp
void recursive_bezier(double x1, double y1,
                      double x2, double y2,
                      double x3, double y3,
                      double x4, double y4)
{
    // Calculate all the mid-points of the line segments
    //----------------------
    double x12   = (x1 + x2) / 2;
    double y12   = (y1 + y2) / 2;
    double x23   = (x2 + x3) / 2;
    double y23   = (y2 + y3) / 2;
    double x34   = (x3 + x4) / 2;
    double y34   = (y3 + y4) / 2;
    double x123  = (x12 + x23) / 2;
    double y123  = (y12 + y23) / 2;
    double x234  = (x23 + x34) / 2;
    double y234  = (y23 + y34) / 2;
    double x1234 = (x123 + x234) / 2;
    double y1234 = (y123 + y234) / 2;
    if(curve_is_flat)
    {
        // Draw and stop
        //----------------------
        draw_line(x1, y1, x4, y4);
    }
    else
    {
        // Continue subdivision
        //----------------------
        recursive_bezier(x1, y1, x12, y12, x123, y123, x1234, y1234);
        recursive_bezier(x1234, y1234, x234, y234, x34, y34, x4, y4);
    }
}
```

> 就这些！这就是你画一条贝塞尔曲线所需要的一切。不过，魔鬼就在“当曲线是平坦”（代码中的curve_is_flat）的这个条件上。

#### 什么时候平坦

最简单的方法是，直接计算点1和点4之间的距离。这种方法不好，因为点1和点4一开始就可能是重合的，当然，有补救的办法，那就是第一次细分的时候我们强制进行进行。

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543938.png)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543940.png)

```cpp
void recursive_bezier(double x1, double y1,
                      double x2, double y2,
                      double x3, double y3,
                      double x4, double y4)
{
    // Calculate all the mid-points of the line segments
    //----------------------
    double x12   = (x1 + x2) / 2;
    double y12   = (y1 + y2) / 2;
    double x23   = (x2 + x3) / 2;
    double y23   = (y2 + y3) / 2;
    double x34   = (x3 + x4) / 2;
    double y34   = (y3 + y4) / 2;
    double x123  = (x12 + x23) / 2;
    double y123  = (y12 + y23) / 2;
    double x234  = (x23 + x34) / 2;
    double y234  = (y23 + y34) / 2;
    double x1234 = (x123 + x234) / 2;
    double y1234 = (y123 + y234) / 2;
    // Try to approximate the full cubic curve by a single straight line
    //------------------
    double dx = x4-x1;
    double dy = y4-y1;
    double d2 = fabs(((x2 - x4) * dy - (y2 - y4) * dx));
    double d3 = fabs(((x3 - x4) * dy - (y3 - y4) * dx));
    if((d2 + d3)*(d2 + d3) < m_distance_tolerance * (dx*dx + dy*dy))
    {
        add_point(x1234, y1234);
        return;
    }
    // Continue subdivision
    //----------------------
    recursive_bezier(x1, y1, x12, y12, x123, y123, x1234, y1234);
    recursive_bezier(x1234, y1234, x234, y234, x34, y, x34, y34, x4, y4);
}
void bezier(double x1, double y1,
            double x2, double y2,
            double x3, double y3,
            double x4, double y4)
{
    add_point(x1, y1);
    recursive_bezier(x1, y1, x2, y2, x3, y3, x4, y4);
    add_point(x4, y4);
}
```

### Estimation of Tangent Error

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543936.png)

> 这个近似中用了36段线段，最大的误差是0.08个像素。
>
> 很显示，仅仅使用长度来估计是不够的。为了让宽画笔（wide strokes）在任何角度都看起来平滑，我们应该对曲率进行估计，而不考虑实际的长度。
>
> 细分后，新产生的两条曲线会比原来的曲线平坦。同时点1和点4的角度也一样。如果它们很不一样，曲线在这个点有一个很急的弯曲，那么就要继续进行细分。

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528019.png)

```cpp
// If the curvature doesn't exceed the distance_tolerance value
// we tend to finish subdivisions.
//----------------------
if(m_angle_tolerance < curve_angle_tolerance_epsilon)
{
    m_points.add(point_type(x1234, y1234));
    return;
}

// Angle & Cusp Condition
//----------------------
double a23 = atan2(y3 - y2, x3 - x2);
double da1 = fabs(a23 - atan2(y2 - y1, x2 - x1));
double da2 = fabs(atan2(y4 - y3, x4 - x3) - a23);
if(da1 >= pi) da1 = 2*pi - da1;
if(da2 >= pi) da2 = 2*pi - da2;
if(da1 + da2 < m_angle_tolerance)
{
    // Finally we can stop the recursion
    //----------------------
    m_points.add(point_type(x1234, y1234));
    return;
}
```

### Processing of the Cusps

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528025.png)

一个三次的（贝塞尔）曲线可以产生一个尖端（cusp），尖端是曲线上切线不连续的点。这种点会使曲线产生一个锐利的转弯，这种转弯无论你怎么放大曲线，看起来都一样的急（Sharp turn）。换句话说，在这个点附近，mitter-join stroke 不可能看起来平滑，因为切线不连续（译注：导数不连续），所以stoke也会产生非常锐利的转弯。在这种情况下，理论上 da1 + da2 < m_angle_tolerance 是不可能产生的，实践中，只要4个点重合在一起，那么条件就成立了，所有atan2调用的参数都会是 0 。这时，递归层次会非常非常深，很可能会产生一个栈益出。那很不好，不过好在有一个很简单的解决办法：

```javascript
if(da1 > m_cusp_limit)
{
    m_points.add(point_type(x2, y2));
    return;
}
if(da2 > m_cusp_limit)
{
    m_points.add(point_type(x3, y3));
    return;
}
```

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543943.jpg)

### Collinear Case

2--------1---------4----------3

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528024.jpg)

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152543948.png)

添加点1234并不够好。In this case the curve is circumscribed and the polyline is escribed. 如果我们添加点23，我们可以得到更好的结果。假设我们只进行一次细分，比较一下两者的结果：

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528022.png)

这里，蓝色的线是我们用点1234产生的，而绿色的则是使用点23产生的。很明显，绿色线的误差要小一些，在实际中，大约是1.5或2倍的差别。

### mattdesl/adaptive-bezier-curve

![图片](https://yin-qin.oss-accelerate.aliyuncs.com/img/20260902152528014.png)

## 神经网络

Scene Text Spotting:Adaptive Bezier-Curve Network
